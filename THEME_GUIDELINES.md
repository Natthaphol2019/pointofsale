# 🎨 MoopikPOS - Theme & UX/UI Guidelines

เอกสารนี้ใช้สำหรับกำหนดมาตรฐานการออกแบบ (Design System) ของระบบ MoopikPOS เพื่อให้ทั้งฝั่ง **Admin (หลังร้าน)** และ **Staff (หน้าร้าน)** มีหน้าตาที่สวยงาม ไปในทิศทางเดียวกัน และใช้งานง่ายที่สุด (Mobile-First & Touch-Friendly)

---

## 1. 🎨 Color Palette (โทนสีหลัก)
เราจะเน้นโทนสีที่ดูสะอาดตา (Clean) และกระตุ้นความน่ากิน (Appetizing) สไตล์แอป Food Delivery ชั้นนำ

- **Primary / Brand (`bg-pos-brand`)**: สีส้มอมแดง (เช่น `#FF5722` หรือ `#F97316`) - ใช้สำหรับปุ่มหลัก, เน้นชื่อเมนู, หรือสถานะสำคัญ
- **Background (`bg-pos-bg`)**: สีเทาอ่อนมาก (`#F3F4F6` หรือ `#F8FAFC`) - สีพื้นหลังจอ เพื่อให้การ์ดสีขาวโดดเด่นขึ้นมา
- **Card / Surface (`bg-pos-card`)**: สีขาวมุก (`#FFFFFF`) - พื้นหลังของกล่องเมนู, ตะกร้า, และหน้าต่าง Modal
- **Text Primary (`text-pos-text`)**: สีเทาเข้มเกือบดำ (`#1F2937`) - อ่านง่าย สบายตา
- **Text Muted (`text-pos-text-muted`)**: สีเทากลาง (`#6B7280`) - สำหรับข้อความรอง, คำอธิบาย
- **Success (`bg-pos-success`)**: สีเขียวสดใส (`#10B981`) - สำหรับปุ่มจ่ายเงิน, บันทึกสำเร็จ
- **Danger (`bg-pos-danger`)**: สีแดง (`#EF4444`) - สำหรับปุ่มลบ, ยกเลิกบิล

---

## 2. 🔤 Typography (ตัวอักษร & Fluid Typography)
- **ฟอนต์หลัก**: `Kanit` หรือ `Prompt` (ผ่าน Google Fonts) เพื่อความโค้งมน เป็นมิตร และอ่านภาษาไทยได้ชัดเจนแม้ตัวเล็ก
- **Fluid Typography**: รองรับการย่อขยายหน้าจออย่างสมูท (ใช้ `clamp()` หรือ Tailwind plugin) ตัวอักษรจะปรับขนาดตามขนาดของอุปกรณ์หรือระดับการซูมอัตโนมัติ โดยไม่ทำให้ Layout พัง
- **ขนาด**: เน้นให้ไซส์เริ่มต้นใหญ่กว่าเว็บทั่วไป 10-15% เพื่อให้พนักงานจิ้มจอได้ง่าย ไม่ต้องเพ่ง และลดการต้องซูมเข้าซูมออกเอง

---

## 2.5 📱 Mobile & Touch Optimizations (ปรับจูนสำหรับพนักงาน iOS / Android)
เพื่อให้การใช้งานบนโทรศัพท์มือถือและแท็บเล็ตให้ความรู้สึกเหมือน "แอปพลิเคชัน (Native App)" จริงๆ:
- **Safe Area Insets**: รองรับขอบจอแหว่ง (Notch) และแถบด้านล่าง (Home Indicator) บน iOS/Android ด้วย `env(safe-area-inset-*)`
- **Fast Tapping (`touch-action: manipulation`)**: ป้องกันการซูมเมื่อเคาะหน้าจอเบิ้ล (Double-tap to zoom) เพื่อให้กดสั่งเมนู หรือปุ่ม + / - สินค้าได้รัวและตอบสนองไวที่สุด
- **Prevent Rubber-Banding (`overscroll-none`)**: ป้องกันหน้าจอเด้งเวลาไถสุดขอบ (Pull-to-refresh / Bounce) เพื่อให้ระบบนิ่งเวลาเลื่อนดูเมนู
- **No Text Selection (`user-select-none`)**: ปิดการคลุมดำและลดอาการเด้งของเมนู Copy/Paste เวลาพนักงานเผลอกดค้างที่รูปอาหาร
- **Smart Keyboards**: ใช้ `inputmode` ที่เหมาะสม (เช่น นัมแพดสำหรับใส่ตัวเลข) บนช่องกรอกข้อความ เพื่อให้คีย์บอร์ดที่เด้งขึ้นมาสะดวกขึ้น

---

## 3. 🧩 UI Components (ชิ้นส่วนหน้าจอ)

### 3.1 Buttons (ปุ่มกด)
- **Shape**: โค้งมนแบบหนา (`rounded-xl` หรือ `rounded-2xl`) ดูเป็นมิตรและทันสมัย
- **Interaction**: เมื่อเอานิ้วแตะ/คลิก จะต้องมีการยุบตัวเล็กน้อย (`active:scale-95`) เพื่อให้ความรู้สึกตอบสนอง (Tactile feedback) และมี Transition นุ่มๆ (`transition-all duration-200`)
- **Shadow**: ปุ่มหลักจะมีเงาบางๆ (`shadow-md`) เพื่อให้ดูนูนขึ้นมาน่ากด

### 3.2 Cards (การ์ดเมนูอาหาร / โต๊ะ)
- กรอบมน (`rounded-2xl`), พื้นขาว, มีเส้นขอบบางๆ (`border-pos-border`) 
- เมื่อ Hover หรือคลิก จะมีเงาพุ่งขึ้นมา และขอบเปลี่ยนเป็นสี Primary

### 3.3 Modals & Bottom Sheets (หน้าต่างเด้ง)
- **หน้าร้าน (Mobile)**: ใช้รูปแบบ **Bottom Sheet** (สไลด์ขึ้นจากขอบล่าง) ให้พนักงานใช้นิ้วโป้งเอื้อมถึงง่าย
- **หลังร้าน (Desktop/Admin)**: ใช้ **Center Modal** วางกลางจอ พร้อมฉากหลังเบลอ (Glassmorphism `backdrop-blur-sm`) และมี Animation ค่อยๆ ซูมขึ้นมา (`animate-in zoom-in`)

### 3.4 Inputs (ช่องกรอกข้อความ/ฟอร์ม)
- ช่องกรอกข้อมูลต้องมีขนาดใหญ่ (Height `h-12` หรือ `h-14`) 
- พื้นหลังสีเทาอ่อนสุด ขอบมน `rounded-xl` 
- เมื่อถูก Focus ขอบจะเรืองแสงเป็นสี Primary

---

## 4. 📐 Layout Structure & Fluid Design (โครงสร้างเลย์เอาต์ที่ยืดหยุ่น)

เพื่อให้รองรับตั้งแต่จอมือถือเล็กๆ ไปจนถึงจอ iPad และจอคอมพิวเตอร์ เราจะใช้โครงสร้างแบบ **Fluid + Responsive Grid**:

### 📏 พื้นฐานเลย์เอาต์ (Universal Scale)
- **Fluid Padding/Margin**: ระยะห่างขอบจอและช่องว่างต่างๆ จะขยายหรือหดตามขนาดจออัตโนมัติ (เช่น ใช้ค่า padding น้อยๆ บนมือถือ และใหญ่ขึ้นบนแท็บเล็ต) ไม่ให้แบนติดขอบและไม่ให้เทอะทะ
- **Minimum Touch Target**: ทุกปุ่มและจุดที่กดได้ (เช่น ไอคอน, เช็คบ็อกซ์) ต้องมีขนาดพื้นที่สัมผัสไม่ต่ำกว่า `44x44px` (ตามมาตรฐาน Apple iOS/Google Android) เพื่อไม่ให้พนักงานจิ้มพลาดไปโดนปุ่มข้างๆ
- **Sticky Elements**: ส่วนหัวสำหรับนำทาง (Header) และแถบล่าง (Bottom Nav / Cart Bar) จะตรึงอยู่กับที่ (Fixed) ให้ใช้นิ้วโป้งเอื้อมถึงเสมอ เวลาไถจอส่วนที่เลื่อนจะมีแค่เนื้อหาตรงกลาง

### 👩‍🍳 ฝั่ง Staff (หน้าร้าน - เน้น Mobile & Tablet)
- **ผังโต๊ะ (Fluid Grid)**: จัดเรียงเป็น Grid แบบยืดหยุ่น (เช่น 2 แถวบนมือถือ, 4 แถวบน iPad) ขยายการ์ดโต๊ะให้เต็มพื้นที่อัตโนมัติ จิ้มง่าย แยกสีสถานะโต๊ะชัดเจน
- **หน้าสั่งอาหาร**: 
  - *มือถือ (Mobile-First)*: แสดงเมนู 1-2 คอลัมน์แบบเต็มจอ หมวดหมู่ (Categories) เป็นแถบเลื่อนซ้ายขวาด้านบนโดยซ่อน Scrollbar เอาไว้ ตะกร้าเป็น **Floating Bottom Bar** ลอยติดขอบล่างคล้ายแอปไลน์แมน
  - *แท็บเล็ต/จอใหญ่ (Split Layout)*: จะแตก Layout เป็น Split Screen แยกระหว่างฝั่งซ้าย (เมนูอาหาร 2/3) และฝั่งขวา (ตะกร้า 1/3 ตรึงโชว์ตลอดเวลาโดยไม่ต้องกดปุ่มเปิด)

### 👨‍💼 ฝั่ง Admin (หลังร้าน - เน้น iPad / Desktop)
- **Navigation (Sidebar)**: ใช้แถบเมนูด้านซ้ายที่พับเก็บได้อัตโนมัติเมื่อจอเล็ก
- **Data Table (ตารางยืดหยุ่น)**: หน้าคลังเมนูใช้ตารางจัดการข้อมูล แต่ถ้ามาเปิดบนมือถือจะถูกพับเป็นรูปแบบ การ์ด (Card View) แทน เพื่อป้องกันไม่ให้ตารางล้นจอจนต้องเลื่อนซ้ายขวาลำบาก
- **Forms (ฟอร์มกรอกข้อมูล)**: หน้าเพิ่มเมนูหรือพนักงาน หากเป็นจอใหญ่จะแบ่งเป็น 2 ฝั่ง (เช่น อัปโหลดรูปซ้าย กรอกข้อมูลขวา) แต่ถ้าจอเล็กจะพับเรียงลงมาเป็นแนวตั้งคอลัมน์เดียวให้กรอกง่ายๆ

---

## 5. ✨ แผนการปรับปรุง (Next Steps)
หากยืนยันตาม Theme นี้ ระบบจะดำเนินการ:
1. ตั้งค่า CSS Variables (สีและฟอนต์) ใน `globals.css` และ `tailwind.config.ts` ให้ใช้ตัวแปรเดียวกันทั้งโปรเจกต์
2. รื้อหน้า Admin (หน้าตั้งค่าเมนู, ท็อปปิ้ง, พนักงาน) ให้สวยงาม โค้งมนเข้ากับหน้าร้าน
3. เกลาหน้า Staff (แผนผังโต๊ะ, ตะกร้า) ให้เนี้ยบกริบระดับแอปมือถือ 100%